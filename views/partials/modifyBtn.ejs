<div class="btn-group" role="group">
	<!-- how do i reuse code here 🤔 -->
	<% let lmao = "/" + link.reduce((t, l) => t + "/" + l.toString()); %> 
	<a href=<%= "/update" + lmao %>
		 type="button" class="btn btn-primary">Edit</a>
	<a href=<%= "/delete" + lmao %>
		type="button" class="btn btn-danger"
		onclick="sillyBilly()">
			<i class="bi bi-trash-fill"></i>
	</a>
</div>
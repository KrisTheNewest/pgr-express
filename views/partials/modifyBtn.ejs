<div class="btn-group" role="group">
	<!-- how do i reuse code here 🤔 -->
	<% let list = "/" + link.reduce((t, l) => t + "/" + l.toString()); %>
	<a href=<%= "/update" + list %> type="button" class="btn btn-primary btn-sm">Edit</a>
	<button type="button" class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#staticBackdrop" onclick="passData('/delete' + '<%-list%>','<%-link[0]%>' )">
	\_/
	</button>
</div>